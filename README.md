# Sleek MERN-style Calculator (no DB)


This repository contains a simple classic calculator built with React (frontend). The calculator performs all operations locally in the browser — no backend is required.

Quick start

1. Open a terminal.
2. Run the client:

```bash
cd client
npm install
npm run dev
```

Open the app at `http://localhost:5173`.
